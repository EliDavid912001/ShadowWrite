# העתק ל-.env מקומי או הוסף ב-Render → Environment

# 4 תשובות — POST /api/scenario
GEMINI_API_KEY=your_gemini_api_key_here
# /api/scenario + /api/chat (מאיה) + /api/feedback (מאמן) — default gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash

# אופציונלי — רק alpha-sim (לא סימולטור מאיה)
# GROQ_API_KEY=gsk_xxxxxxxx

PORT=3000
